import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.music.com',
  appName: 'npm run buildnpx cap add android',
  webDir: 'dist'
};

export default config;
